// Configuration for the app

export const API_BASE_URL = 'http://localhost:4000';
export const WS_BASE_URL = 'ws://localhost:4000/ws/reports';

// MOCK_MODE allows the app to function without a backend during development.
// It intercepts failed network requests and returns mock data instead.
export const MOCK_MODE = true; 
