name: railsetu_field_app
description: RailSetu Field Inspector App — Railway safety reporting for field workers.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  uuid: ^4.5.1
  intl: ^0.19.0
  provider: ^6.1.2
  google_fonts: ^6.2.1
  geolocator: ^14.0.3
  image_picker: ^1.2.3
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
